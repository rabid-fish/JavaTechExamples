package com.github.rabid_fish.example3

import com.github.rabid_fish.MathResult;

rule "Stateful Simple Example Less Than 5"
	when
        result : MathResult( result.value < 5 )

	then
		System.out.println( "Value is less than 5" );
		modify( result ) { setValue( result.getValue() + 1 ) };
	
end
